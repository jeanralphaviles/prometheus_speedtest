language: python
os: linux
python:
  - "3.7-dev"
services:
  - docker
install:
  - pip install pylint yapf -r requirements.txt
  - gem install mdl
script:
  - docker build .
  - pylint **/*.py
  - pytest
  - yapf -d **/*.py
  - mdl README.md
